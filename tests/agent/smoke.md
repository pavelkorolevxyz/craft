Короткая проверка Craft, не полноценная сборка. Навык только для чтения лежит в ./craft. Прочитай craft/SKILL.md и только нужные ветки. Пиши только в ./output, не читай другие навыки и не устанавливай зависимости.

Создай минимальные стартеры в output/slides и output/interface. Через штатный CLI выясни доступные фрагменты слайдов и запроси контракт сравнения двух способов проверки. Не угадывай идентификатор по имени файла.

Дай решения трёх вопросов:
1. На слайде две независимые задачи, обучение команды и мониторинг. Какую раскладку взять?
2. На слайде клиент отправляет запрос серверу, тот передаёт событие журналу. Узлы равноправны, докладчик показывает путь по частям. Что видно сначала, затем, какой узел выделен?
3. Готовый компонент интерфейса имеет фон --raise. Нужно ли сделать его прозрачным локальным CSS ради соответствия Craft?

Сохрани output/decisions.json с полями comparisonFragment, comparisonFile, independentLayout, flowFrames, flowAccent, backgroundDecision и commands. Последнее поле содержит фактически выполненные команды поиска фрагментов. Не создавай содержательную колоду и не выполняй полный визуальный прогон. Заверши коротким ответом.
